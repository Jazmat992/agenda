import { TotalStoragePipe } from './total-storage.pipe';

describe('TotalStoragePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalStoragePipe();
    expect(pipe).toBeTruthy();
  });
});
