import { TableDataPipePipe } from './table-data-pipe.pipe';

describe('TableDataPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TableDataPipePipe();
    expect(pipe).toBeTruthy();
  });
});
